
{{>_msg}}
{{#each lista}}
<div class="card mt-2">
    <div class="card-body" style="align-items: center;justify-content: center; display: flex;">
        <svg xmlns="http://www.w3.org/2000/svg" width="50" height="50" fill="currentColor" class="bi bi-person-lines-fill" viewBox="0 0 16 16">
            <path d="M6 8a3 3 0 1 0 0-6 3 3 0 0 0 0 6zm-5 6s-1 0-1-1 1-4 6-4 6 3 6 4-1 1-1 1H1zM11 3.5a.5.5 0 0 1 .5-.5h4a.5.5 0 0 1 0 1h-4a.5.5 0 0 1-.5-.5zm.5 2.5a.5.5 0 0 0 0 1h4a.5.5 0 0 0 0-1h-4zm2 3a.5.5 0 0 0 0 1h2a.5.5 0 0 0 0-1h-2zm0 3a.5.5 0 0 0 0 1h2a.5.5 0 0 0 0-1h-2z"></path>
        </svg>

    </div>
    <div class="card-body mt-1">
        <div class="card card-header">
            <h4>{{nome}}</h4>
        </div>
        <div class="card card-header mt-1">
            <h4>{{telefone}}</h4>
        </div>
        <small>você adicionou esse contato em: {{date}}</small>
        <table class="mt mt-2" style="align-items: center;justify-content: center; display: flex;">
            <tr>
                <td>
                    <a href="/edita_agenda/{{_id}}"><button class="btn btn-primary">Editar</button></a>
                </td>
                
                <td>
                    <form method="post" action="/deleta_agenda">
                        <input type="hidden" name="id" value="{{_id}}">
                        <button type="submit" class="btn btn-primary">Deletar</button>
                        
                    </form>
                </td>
            </tr>
        </table>
        
        
        

    </div>
</div>
{{else}}

{{/each}}